# Proposal Generator - Environment Configuration

# Google Gemini API Key (required for LLM narrative generation)
GEMINI_API_KEY=your-gemini-api-key-here

# Groq API Key (optional - fallback when Gemini quota exceeded)
GROQ_API_KEY=your-groq-api-key-here

# Default proposal validity period in days
DEFAULT_VALIDITY_DAYS=14

# Default platform (upwork | direct)
DEFAULT_PLATFORM=direct

# Producer/Company information (can be overridden per proposal)
PRODUCER_NAME=Your Company Name
PRODUCER_EMAIL=contact@example.com
PRODUCER_DOMAIN=example.com

# CTA defaults (optional - leave blank if not using)
CTA_CALENDLY_LINK=
